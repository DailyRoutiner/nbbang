<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<resultMap type="memDTO" id="memResultMap">
		<result column="memno" property="memno" />
		<result column="memname" property="memname" />
		<result column="mempw" property="mempw" />
		<result column="cardpw" property="cardpw" />
		<result column="email" property="email" />
		<result column="account" property="account" />
		<result column="phonenumber" property="phonenumber" />
		<result column="deviceid" property="deviceid" />
		<result column="mempic" property="mempic" />
	</resultMap>

	<select id="memSelect" resultMap="memResultMap">
		select * from member 
	</select>
	<select id="memJoinCheck" parameterType="String" resultMap="memResultMap">
		select * from member where email = #{email}
	</select>
	<select id="memEmailCheck" parameterType="memDTO" resultMap="memResultMap">
		select * from member where email = #{email} and mempw = #{mempw} 
	</select>
	<insert id ="insertMember" parameterType = "memDTO">
<<<<<<< HEAD
  		insert into member(memno, memname, mempw, email, deviceid, phonenumber) values(#{memno}, #{memname}, #{mempw}, #{email},#{deviceid}, #{phonenumber})
=======
  		insert into member(memno, memname, mempw, email, deviceid, phonenumber) values(((select max(memno)from member)+1),#{memname}, #{mempw}, #{email},#{deviceid}, #{phonenumber})
>>>>>>> master
  	</insert>
  	
  	<update id = "updatePhone" parameterType = "memDTO">
  		update member set phonenumber=#{phonenumber} where email=#{email}
  	</update>
<<<<<<< HEAD
=======
  	
  	<update id = "updatePicture" parameterType = "memDTO">
  		update member set mempic=#{mempic} where memno = #{memno}
  	</update>
  	
  	<update id = "updateName" parameterType = "memDTO">
  		update member set memname=#{memname} where email=#{email}
  	</update>
  	
  	<update id = "updatePassWord" parameterType = "memDTO">
  		update member set mempw=#{mempw} where email = #{email}
  	</update>
  	
  	<update id = "updateCardPassWord" parameterType = "memDTO">
  		update member set cardpw=#{cardpw} where email = #{email}
  	</update>
  	
  	<update id = "updateAccount" parameterType = "memDTO">
  		update member set account=#{account} where email = #{email}
  	</update>
  	
  	<update id = "updateDeviceId" parameterType = "memDTO">
  		update member set deviceid=#{deviceid} where email = #{email}
  	</update>
  	 <delete id = "deleteMember" parameterType = "memDTO">
  		delete from member where email=#{email}
  	</delete>
>>>>>>> master
</mapper>
